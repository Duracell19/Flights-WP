<views:MvxWindowsPage
    x:Class="Flights.WP.Views.FlightsInfoView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:views="using:MvvmCross.WindowsCommon.Views"
    mc:Ignorable="d">

    <Grid HorizontalAlignment="Center">
        <ListView ItemsSource="{Binding InfoFlyList}" >
            <ListView.ItemTemplate>
                <DataTemplate>
                    <StackPanel>
                        <TextBlock Text=""/>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Style="{StaticResource TextBlockInfoFlyStyle}" 
                                       Text="{Binding StaticInformation}"/>
                            <Image Source="ms-appx:///Assets/direction.png" 
                                   Width="30"/>
                            <TextBlock Style="{StaticResource TextBlockInfoFlyStyle}" 
                                       Text="{Binding Information}"/>
                        </StackPanel>
                        <TextBlock Text=""/>
                    </StackPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
    
</views:MvxWindowsPage>
