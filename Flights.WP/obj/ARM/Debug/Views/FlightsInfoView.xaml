<views:MvxWindowsPage
    x:Class="Flights.WP.Views.FlightsInfoView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:views="using:MvvmCross.WindowsCommon.Views"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid Grid.Column="0" Grid.Row="0" Grid.ColumnSpan="3">
            <ListView ItemsSource="{Binding InfoFlyList}" >
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Vertical">
                            <TextBlock Text=""/>
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Style="{StaticResource TextBlockInfoFlyStyle}" Text="{Binding StaticInformation}"/>
                                <Image Source="ms-appx:///Assets/direction.png" Width="30"/>
                                <TextBlock Style="{StaticResource TextBlockInfoFlyStyle}" Text="{Binding Information}"/>
                            </StackPanel>
                            <TextBlock Text=""/>
                        </StackPanel>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </Grid>
        <Button Style="{StaticResource ButtonStyle}" Grid.Column="1" Grid.Row="1" Content="Return to flight list" Command="{Binding BackCommand}"/>
    </Grid>

</views:MvxWindowsPage>

